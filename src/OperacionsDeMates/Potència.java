package OperacionsDeMates;

public class Potència {
    //Potencia (2 elevat  4)
    double x= Math.pow(2,4);
    int y= (int)Math.pow(2,4);

    // Arrelquaddrada
    double a= Math.sqrt(16);

// Constant PI
double radi=100;
double ac= Math.PI*Math.pow(radi,2);

// Arrodonir numero
int nota= (int)Math.round(4.7);

// Numero aleatori
    double r= 10+Math.random();
}
